net.mptcp.enabled = 1
# Only accepted by the custom kernel (linux-mptcp-redundant); on the stock
# kernel this line fails harmlessly until the first boot into the new kernel.
net.mptcp.scheduler = redundant
